<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="160dp"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="4dp"
    app:cardBackgroundColor="?attr/colorSurface">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="140dp"
        android:orientation="vertical"
        android:padding="16dp"
        android:background="@drawable/bg_ruta_item">

        <!-- Origen -->
        <TextView
            android:id="@+id/tvOrigen"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Origen"
            android:textSize="12sp"
            android:textColor="?attr/android:textColorSecondary"
            android:maxLines="1"
            android:ellipsize="end"
            android:drawableStart="@drawable/ic_location_from"
            android:drawablePadding="4dp"
            android:drawableTint="?attr/android:textColorSecondary"/>

        <!-- Destino -->
        <TextView
            android:id="@+id/tvDestino"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Destino"
            android:textSize="14sp"
            android:textColor="@color/primary_500"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end"
            android:layout_marginTop="8dp"
            android:drawableStart="@drawable/ic_location_to"
            android:drawablePadding="4dp"
            android:drawableTint="@color/primary_500"/>

        <!-- Separador -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="?attr/colorSurfaceVariant"
            android:layout_marginVertical="8dp"/>

        <!-- Horario -->
        <TextView
            android:id="@+id/tvHorario"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="--:--"
            android:textSize="12sp"
            android:textColor="?attr/android:textColorSecondary"
            android:gravity="center_vertical"
            android:drawableStart="@drawable/ic_time"
            android:drawablePadding="4dp"
            android:drawableTint="?attr/android:textColorSecondary"/>

        <!-- Precio -->
        <TextView
            android:id="@+id/tvPrecioRuta"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="$0"
            android:textSize="12sp"
            android:textColor="@color/warning_500"
            android:textStyle="bold"
            android:gravity="center_vertical"
            android:layout_marginTop="4dp"
            android:drawableStart="@drawable/ic_price"
            android:drawablePadding="4dp"
            android:drawableTint="@color/warning_500"/>

        <!-- Badge Siguiente (Feedback Conductor) -->
        <TextView
            android:id="@+id/tvBadgeProximoRuta"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:background="@drawable/bg_status_next"
            android:paddingHorizontal="8dp"
            android:paddingVertical="2dp"
            android:text="@string/badge_siguiente"
            android:textColor="@color/white"
            android:textSize="10sp"
            android:textStyle="bold"
            android:visibility="gone"
            tools:visibility="visible" />

    </LinearLayout>

</androidx.cardview.widget.CardView>
