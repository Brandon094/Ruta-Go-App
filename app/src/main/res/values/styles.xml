<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- ==================== -->
    <!-- SISTEMA BASE DE BOTONES - ESTILOS UNIFICADOS -->
    <!-- ==================== -->

    <!--
        ESTILO BASE - TODOS LOS BOTONES HEREDAN DE AQUÍ
        Modifica esto y todos los botones se actualizarán
    -->
    <style name="AppButtonBase" parent="Widget.MaterialComponents.Button">
        <!-- TAMAÑOS BASE -->
        <item name="android:layout_height">48dp</item>
        <item name="android:minHeight">48dp</item>

        <!-- TIPOGRAFÍA BASE -->
        <item name="android:textSize">16sp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textAppearance">@style/TextAppearance.MaterialComponents.Body1</item>
        <item name="android:letterSpacing">0</item>
        <item name="android:fontFamily">sans-serif</item>

        <!-- FORMA BASE (esquinas redondeadas) -->
        <item name="cornerRadius">24dp</item>

        <!-- SOMBRAS Y ELEVACIÓN -->
        <item name="elevation">2dp</item>

        <!-- ÍCONOS (configuración base) -->
        <item name="iconPadding">8dp</item>
        <item name="iconSize">20dp</item>
    </style>

    <!-- ==================== -->
    <!-- VARIANTES PRINCIPALES -->
    <!-- ==================== -->

    <!-- 1. BOTÓN PRIMARIO (relleno con color primario) -->
    <style name="AppButton.Primary" parent="Widget.MaterialComponents.Button">
        <!-- TAMAÑOS -->
        <item name="android:layout_height">48dp</item>
        <item name="android:minHeight">48dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textAllCaps">false</item>
        <item name="cornerRadius">24dp</item>

        <!-- COLORES - VALORES FIJOS -->
        <item name="backgroundTint">@color/primary_500</item>
        <item name="android:textColor">@color/white</item>
        <item name="iconTint">@color/white</item>
        <item name="rippleColor">@color/primary_300</item>

        <!-- ELEVACIÓN -->
        <item name="elevation">4dp</item>

        <!-- ÍCONOS -->
        <item name="iconPadding">8dp</item>
        <item name="iconSize">20dp</item>
    </style>

    <!-- 2. BOTÓN SECUNDARIO (outline/borde) -->
    <style name="AppButton.Secondary" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <!-- TAMAÑOS -->
        <item name="android:layout_height">48dp</item>
        <item name="android:minHeight">48dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textAllCaps">false</item>
        <item name="cornerRadius">24dp</item>

        <!-- COLORES - VALORES FIJOS (los selectores ya usan colores fijos) -->
        <item name="strokeColor">@color/secondary_stroke_selector</item>
        <item name="strokeWidth">1.5dp</item>
        <item name="android:textColor">@color/secondary_text_selector</item>
        <item name="iconTint">@color/secondary_text_selector</item>
        <item name="backgroundTint">@android:color/transparent</item>
        <item name="rippleColor">@color/primary_100</item>
        <item name="elevation">0dp</item>
    </style>

    <!-- 3. BOTÓN TEXTO (solo texto, sin fondo) -->
    <style name="AppButton.Text" parent="Widget.MaterialComponents.Button.TextButton">
        <!-- TAMAÑOS -->
        <item name="android:layout_height">48dp</item>
        <item name="android:minHeight">48dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textAllCaps">false</item>
        <item name="cornerRadius">24dp</item>

        <!-- COLORES - VALORES FIJOS -->
        <item name="android:textColor">@color/primary_500</item>
        <item name="iconTint">@color/primary_500</item>
        <item name="rippleColor">@color/primary_100</item>
        <item name="backgroundTint">@android:color/transparent</item>
        <item name="elevation">0dp</item>
    </style>

    <!-- ==================== -->
    <!-- ESTILOS DE TEXTO (TAMAÑOS) -->
    <!-- ==================== -->

    <!-- Text.XSmall -->
    <style name="AppButton.Text.XSmall" parent="AppButton.Text">
        <item name="android:layout_height">32dp</item>
        <item name="android:minHeight">32dp</item>
        <item name="android:textSize">12sp</item>
        <item name="cornerRadius">16dp</item>
        <item name="iconSize">16dp</item>
        <item name="iconPadding">4dp</item>
    </style>

    <!-- Text.Small -->
    <style name="AppButton.Text.Small" parent="AppButton.Text">
        <item name="android:layout_height">36dp</item>
        <item name="android:minHeight">36dp</item>
        <item name="android:textSize">14sp</item>
        <item name="cornerRadius">18dp</item>
        <item name="iconSize">18dp</item>
        <item name="iconPadding">6dp</item>
    </style>

    <!-- 4. BOTÓN SUAVE (fondo muy suave) -->
    <style name="AppButton.Soft" parent="AppButtonBase">
        <!-- COLORES - VALORES FIJOS -->
        <item name="backgroundTint">@color/primary_50</item>
        <item name="android:textColor">@color/primary_500</item>
        <item name="iconTint">@color/primary_500</item>
        <item name="rippleColor">@color/primary_100</item>
        <item name="elevation">1dp</item>
    </style>

    <!-- ==================== -->
    <!-- VARIANTES DE TAMAÑO -->
    <!-- ==================== -->

    <!-- TAMAÑO PEQUEÑO -->
    <style name="AppButton.Small">
        <item name="android:layout_height">36dp</item>
        <item name="android:minHeight">36dp</item>
        <item name="android:textSize">14sp</item>
        <item name="cornerRadius">18dp</item>
        <item name="iconSize">18dp</item>
        <item name="iconPadding">6dp</item>
    </style>

    <!-- TAMAÑO EXTRA PEQUEÑO -->
    <style name="AppButton.XSmall">
        <item name="android:layout_height">32dp</item>
        <item name="android:minHeight">32dp</item>
        <item name="android:textSize">12sp</item>
        <item name="cornerRadius">16dp</item>
        <item name="iconSize">16dp</item>
        <item name="iconPadding">4dp</item>
    </style>

    <!-- TAMAÑO GRANDE -->
    <style name="AppButton.Large">
        <item name="android:layout_height">56dp</item>
        <item name="android:minHeight">56dp</item>
        <item name="android:textSize">18sp</item>
        <item name="cornerRadius">28dp</item>
        <item name="iconSize">24dp</item>
        <item name="iconPadding">10dp</item>
    </style>

    <!-- ==================== -->
    <!-- COMBINACIONES COMUNES (tamaño + variante) -->
    <!-- ==================== -->

    <style name="AppButton.Primary.Small" parent="AppButton.Primary">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:minHeight">36dp</item>
        <item name="android:textSize">14sp</item>
        <item name="cornerRadius">18dp</item>
        <item name="iconSize">18dp</item>
        <item name="iconPadding">6dp</item>
    </style>

    <style name="AppButton.Primary.XSmall" parent="AppButton.Primary">
        <item name="android:layout_height">32dp</item>
        <item name="android:minHeight">32dp</item>
        <item name="android:textSize">12sp</item>
        <item name="cornerRadius">16dp</item>
        <item name="iconSize">16dp</item>
        <item name="iconPadding">4dp</item>
    </style>

    <style name="AppButton.Secondary.Small" parent="AppButton.Secondary">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:minHeight">36dp</item>
        <item name="android:textSize">14sp</item>
        <item name="cornerRadius">18dp</item>
        <item name="iconSize">18dp</item>
        <item name="iconPadding">6dp</item>
    </style>

    <style name="AppButton.Secondary.XSmall" parent="AppButton.Secondary">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:minHeight">32dp</item>
        <item name="android:textSize">12sp</item>
        <item name="cornerRadius">16dp</item>
        <item name="iconSize">16dp</item>
        <item name="iconPadding">4dp</item>
    </style>

    <style name="AppButton.Soft.Small" parent="AppButton.Soft">
        <item name="android:layout_height">36dp</item>
        <item name="android:minHeight">36dp</item>
        <item name="android:textSize">14sp</item>
        <item name="cornerRadius">18dp</item>
        <item name="iconSize">18dp</item>
        <item name="iconPadding">6dp</item>
    </style>

    <!-- ==================== -->
    <!-- STYLE PARA FAB - CON VALORES FIJOS (CORREGIDO) -->
    <!-- ==================== -->
    <style name="FabStyle" parent="Widget.MaterialComponents.FloatingActionButton">
        <item name="backgroundTint">@color/primary_500</item>
        <item name="tint">@color/white</item>
        <item name="rippleColor">@color/primary_300</item>
        <item name="elevation">6dp</item>
        <item name="hoveredFocusedTranslationZ">8dp</item>
        <item name="pressedTranslationZ">10dp</item>
        <item name="fabSize">normal</item>
        <item name="borderWidth">0dp</item>
        <item name="useCompatPadding">true</item>
    </style>

    <!-- Versión mini del FAB -->
    <style name="FabStyle.Mini" parent="FabStyle">
        <item name="fabSize">mini</item>
    </style>

    <!-- ==================== -->
    <!-- STYLES PARA BOTONES DE ASIENTOS MEJORADOS -->
    <!-- ==================== -->

    <!-- Estilo base para todos los botones de asiento -->
    <style name="AsientoButtonStyle" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="android:layout_width">45dp</item>
        <item name="android:layout_height">45dp</item>
        <item name="android:padding">0dp</item>
        <item name="android:minWidth">0dp</item>
        <item name="android:minHeight">0dp</item>
        <item name="cornerRadius">10dp</item>

        <!-- Icono -->
        <item name="iconGravity">textStart</item>
        <item name="iconPadding">0dp</item>
        <item name="iconSize">20dp</item>
        <item name="iconTint">@null</item>

        <!-- Colores base - VALORES FIJOS -->
        <item name="backgroundTint">@color/secondary_900</item>
        <item name="strokeColor">@color/outline</item>
        <item name="strokeWidth">1.5dp</item>
        <item name="rippleColor">@color/primary_500</item>
        <item name="elevation">1dp</item>
    </style>

    <!-- Estilo para asiento disponible -->
    <style name="AsientoButtonStyle.Disponible" />

    <!-- Estilo para asiento seleccionado -->
    <style name="AsientoButtonStyle.Seleccionado" parent="AsientoButtonStyle">
        <item name="strokeColor">@color/primary_500</item>
        <item name="strokeWidth">2.5dp</item>
        <item name="backgroundTint">@color/primary_50</item>
        <item name="rippleColor">@color/ripple_primary</item>
        <item name="elevation">2dp</item>
    </style>

    <!-- Estilo para asiento ocupado -->
    <style name="AsientoButtonStyle.Ocupado" parent="AsientoButtonStyle">
        <item name="strokeColor">@color/status_cancelled</item>
        <item name="backgroundTint">@color/status_cancelled_light</item>
        <item name="rippleColor">@color/ripple_color</item>
        <item name="android:enabled">false</item>
    </style>

    <!-- Estilo para asiento de conductor -->
    <style name="AsientoButtonStyle.Conductor" parent="AsientoButtonStyle.Ocupado">
        <item name="backgroundTint">@color/secondary_700</item>
        <item name="strokeColor">@color/secondary_100</item>
        <item name="strokeWidth">1.5dp</item>
    </style>

    <!-- ==================== -->
    <!-- STYLES PARA FILTROS CHIP - CON VALORES FIJOS -->
    <!-- ==================== -->
    <style name="FilterChip" parent="Widget.MaterialComponents.Chip.Filter">
        <item name="chipMinTouchTargetSize">0dp</item>
        <item name="chipStartPadding">12dp</item>
        <item name="chipEndPadding">12dp</item>
        <item name="chipMinHeight">32dp</item>
        <item name="android:textSize">12sp</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="checkedIcon">@null</item>
        <item name="checkedIconVisible">false</item>
        <item name="chipStrokeWidth">1dp</item>
        <item name="chipStrokeColor">@color/outline</item>
    </style>

    <style name="FilterChipSelected" parent="FilterChip">
        <item name="chipBackgroundColor">@color/chip_background_selected</item>
        <item name="android:textColor">@color/chip_text_selected</item>
    </style>

    <!-- ==================== -->
    <!-- STYLES PARA BOTONES PEQUEÑOS - CON VALORES FIJOS -->
    <!-- ==================== -->
    <style name="ButtonOutlineSmall" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="android:layout_height">32dp</item>
        <item name="android:textSize">12sp</item>
        <item name="strokeColor">@color/primary_500</item>
        <item name="android:textColor">@color/primary_500</item>
        <item name="backgroundTint">@android:color/transparent</item>
    </style>

    <style name="ButtonFilledSmall" parent="Widget.MaterialComponents.Button">
        <item name="android:layout_height">32dp</item>
        <item name="android:textSize">12sp</item>
        <item name="backgroundTint">@color/primary_500</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <!-- ==================== -->
    <!-- STYLES PARA CARDS DE RESERVA - CON VALORES FIJOS -->
    <!-- ==================== -->
    <style name="ReservaCardStyle" parent="CardView">
        <item name="cardCornerRadius">12dp</item>
        <item name="cardElevation">2dp</item>
        <item name="cardUseCompatPadding">true</item>
        <item name="cardBackgroundColor">@color/surface</item>
        <item name="strokeColor">@color/outline</item>
        <item name="strokeWidth">1dp</item>
    </style>

    <style name="ReservaCardElevated" parent="ReservaCardStyle">
        <item name="cardElevation">4dp</item>
        <item name="cardMaxElevation">8dp</item>
    </style>

    <!-- ==================== -->
    <!-- STYLES PARA TEXTOS - CON VALORES FIJOS -->
    <!-- ==================== -->
    <style name="TextTitleLarge">
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/text_primary</item>
    </style>

    <style name="TextTitleMedium">
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/text_primary</item>
    </style>

    <style name="TextBodyMedium">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/text_primary</item>
    </style>

    <style name="TextBodySmall">
        <item name="android:textSize">12sp</item>
        <item name="android:textColor">@color/text_secondary</item>
    </style>

    <style name="TextCaption">
        <item name="android:textSize">11sp</item>
        <item name="android:textColor">@color/text_secondary</item>
        <item name="android:letterSpacing">0.02</item>
    </style>

    <!-- ==================== -->
    <!-- STYLES PARA ESTADOS DE RESERVA -->
    <!-- ==================== -->
    <style name="StatusBadgeConfirmed">
        <item name="android:background">@drawable/bg_estado_confirmado</item>
        <item name="android:textColor">@color/status_confirmed</item>
        <item name="android:textSize">10sp</item>
    </style>

    <style name="StatusBadgePending">
        <item name="android:background">@drawable/bg_estado_pendiente</item>
        <item name="android:textColor">@color/status_pending</item>
        <item name="android:textSize">10sp</item>
    </style>

    <style name="StatusBadgeCancelled">
        <item name="android:background">@drawable/bg_estado_cancelado</item>
        <item name="android:textColor">@color/status_cancelled</item>
        <item name="android:textSize">10sp</item>
    </style>

    <!-- ==================== -->
    <!-- STYLES PARA DIVIDERS - CON VALORES FIJOS -->
    <!-- ==================== -->
    <style name="DividerHorizontal">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:background">@color/outline</item>
    </style>

    <style name="DividerVertical">
        <item name="android:layout_width">1dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:background">@color/outline</item>
    </style>

    <!-- ==================== -->
    <!-- SHAPE APPEARANCE OVERLAYS -->
    <!-- ==================== -->
    <style name="ShapeAppearanceOverlay.CardTopRounded" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSizeTopLeft">12dp</item>
        <item name="cornerSizeTopRight">12dp</item>
        <item name="cornerSizeBottomLeft">0dp</item>
        <item name="cornerSizeBottomRight">0dp</item>
    </style>

    <style name="ShapeAppearanceOverlay.CardBottomRounded" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSizeTopLeft">0dp</item>
        <item name="cornerSizeTopRight">0dp</item>
        <item name="cornerSizeBottomLeft">12dp</item>
        <item name="cornerSizeBottomRight">12dp</item>
    </style>

    <!-- ==================== -->
    <!-- ESTILO POR DEFECTO (para compatibilidad) -->
    <!-- ==================== -->
    <style name="AppButton" parent="AppButton.Primary">
        <!-- Este es solo un alias para evitar errores -->
    </style>
</resources>
